alphabet = ['a', 'b', 'c', 'd', 'e']
new_dict = {letter : letter.upper() + "*" for letter in alphabet}
print(new_dict)

user_word = input("Enter a word: ")
user_list = [n for n in user_word if n in alphabet]

secret_code = [new_dict[char] for char in user_list]
print(secret_code)