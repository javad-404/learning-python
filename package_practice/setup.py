from setuptools import setup, find_packages

setup(
      name="mylibrary",
      version="1.0",
      package=find_packages(),
      author="javad",
      description="My first python package for managing books"
      )

