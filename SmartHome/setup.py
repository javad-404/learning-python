from setuptools import setup, find_packages

setup(
      name="my_house",
      version="1.0.0",
      packages=["my_house"],
      author="javad",
      description="devices turn on and off practice"
         )