from setuptools import setup, find_packages

setup(
      name="my_money",
      version="1.0.0",
      #packages=find_packages(),
      packages=["my_money"],
      author="javad",
      description="this is a packages practice" 
      )

